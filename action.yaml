name: "Test Action"
description: "test action"
inputs:
  api_endpoint:
    description: "Boost API endpoint"
    required: true
    default: https://api.boostsecurity.net
  api_token:
    description: "Boost API token"
    required: true
  modules_path:
    description: "Path to the modules directory"
    required: false
    default: "scanners/"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - name: Install poetry
      run: pip3 install --pre "poetry==1.2.0b1"
      shell: bash
    - name: Install dependencies
      run: |
        cp ${{ github.action_path }}/pyproject.toml ${{ github.action_path }}/poetry.lock . && \
          poetry install --no-root --without dev
      shell: bash
    - name: Validate rules database
      run: |
        poetry run python ${{ github.action_path }}/boostsec/registry_validator/validate_rules_db.py \
          --rules-db-path ${{ inputs.modules_path }}
      shell: bash
    - name: Validate namespaces
      run: |
        poetry run python ${{ github.action_path }}/boostsec/registry_validator/validate_namespaces.py \
          --modules-path ${{ inputs.modules_path }}
      shell: bash
    - name: Upload rules database
      if: ${{ github.event_name == 'push' }}
      run: |
        poetry run python ${{ github.action_path }}/boostsec/registry_validator/upload_rules_db.py \
          --modules-path ${{ inputs.modules_path }} --api-endpoint ${{ inputs.api_endpoint }} \
          --api-token ${{ inputs.api_token }}
      shell: bash
